import { FC } from "react";
import HeaderNav from "./HeaderNav";

const Header: FC = () => (
  <header>
    <img alt="" />
    Header Component
    <HeaderNav />
  </header>
);

export default Header;
